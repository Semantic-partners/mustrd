### ⚠️ Duplicate competency questions

These `must:competencyQuestion` values appear on more than one spec (likely a copy/paste error). The affected specs are **excluded from the coverage calculation**:
{% for d in duplicate_cqs %}
- "{{ d.cq }}"
{% for s in d.specs %}  - {% if s.link %}[{{ s.name }}]({{ s.link }}){% else %}{{ s.name }}{% endif %}
{% endfor %}{% endfor %}
